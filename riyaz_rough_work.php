 //return $request->group_id;

        /* Validate group and class_id*/
        // $validator = Validator::make([$request->group_id], [
        //      'group_id' => 'required',
        //      'class_id' => 'required'
        // ]);

        // if ($validator->fails()){

        //     return [
        //         'message' => 'validation_failed',
        //         'errors' => $validator->errors()
        //     ];
        
        // }